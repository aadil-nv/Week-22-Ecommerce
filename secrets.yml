apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-secret
  namespace: default  # You can change the namespace if needed
type: kubernetes.io/basic-auth
data:
  username: YWFkaWxudg==  # Replace with base64 encoded username
  password: QWRpbEA5NTM5NDc=   # Replace with base64 encoded password
